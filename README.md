<HR><!-- -------------------------------------------------------------------------------------- -->
Ken Thompson and Avie Tevanian need to prove the following theorem ...
<P>
<EM>
Linux, Windows, and OSX are the only operating systems, and they are all different.
The only operating system origins are UNIX and DOS, and they are different.
The origin of Linux and OSX is UNIX, and the origin of Windows is DOS.
All computers run one or more operating systems.
A computer benefits from the origins of the operating systems that it runs.
My computer runs OSX.
Therefore my computer benefits from UNIX.
</EM>
<P>
<OL>
<LI> Convert the scenario to <EM>typed</EM> first-order logic formulae in TPTP syntax, and save
     them in the file <TT>PartII.txt</TT>.
<P>
<LI> Check the syntax and format the formulae.
     Save the formatted version back to <TT>PartII.formatted.txt</TT>.
<P>
<LI> Check the types of the formulae.
     Save the output in <TT>PartII.types.txt</TT>.
<P>
<LI> Submit the axioms to a model finder to ensure they are consistent.
     Save the output in <TT>PartII.sat.txt</TT>
<P>
<LI> Submit <TT>PartII.p</TT> to E.
     Obtain TPTP format output, and save the (formatted) TPTP format solution in
     <TT>PartII.thm.txt</TT>.
<P>
<LI> Submit <TT>PartII.thm.txt</TT> for output processing to produce an IDV tree.
     Save the image in <TT>PartII.IDV.png</TT> (or whatever image format you use).
<P>
<LI> Submit <TT>PartII.thm.txt</TT> for output processing to produce a GDV verification trace.
     Save the trace in <TT>PartII.gdv.txt</TT>
</OL>
<P>
<HR><!-- -------------------------------------------------------------------------------------- -->
